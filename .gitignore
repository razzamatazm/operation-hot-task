node_modules
.env
.env.*
dist
build
coverage
.DS_Store
apps/server/data/*.json
apps/server/data/*.db
npm-debug.log*
yarn-error.log*
pnpm-lock.yaml
teams-app/manifest.generated.json
teams-app/*.zip

# TS incremental build info
*.tsbuildinfo

# Throwaway debug harnesses. Disposable by design — they answer one question
# and get deleted, so they never belong in history.
scripts/debug/
