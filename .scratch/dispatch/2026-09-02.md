# Dispatch 2026-09-02

Queue: #166, #172, #179 (all `ready-for-agent`).

Screening:
- #179 — CLOSED as superseded tracker. #194/#196/#197/#198 all merged.
- #172 — HELD. Full spec exists on the issue; it is ~5 separable builds and the
  spec's own instruction is /to-tickets. /to-tickets is disable-model-invocation,
  so the user must run it. Not dispatchable this run.
- #166 — DISPATCHED. Re-briefed first (comment 5519859674) because the Agent
  Brief on the issue described work already shipped in PR #190.

## Lanes

Lane 1: #166 — claimed-time row in the hamburger, read from history.

## /to-tickets on #172 (user-invoked, 2026-09-02)

#172 split into five tickets, all `ready-for-agent`:
- #230 A completion records what was found — no blockers, critical slice (server
  currently drops reviewNotes on COMPLETED)
- #231 The Checked panel — blocked by #230
- #232 Requester's completion DM — blocked by #230, highest value per spec
- #233 Outcome on the closed row — blocked by #230
- #234 Confirm Fixed / send back for re-check — blocked by #231, #233

Merge-order hazard: #231, #233, #234 and the in-flight #166 all touch
apps/web/src/App.tsx. #166 is around the hamburger menu; #231/#233 are around the
quick-action slot and closed row.

#172 itself still carries `ready-for-agent` — the to-tickets skill forbids
modifying a parent issue. Next dispatch will pick it up unless relabelled.

## Lane results

#166 — the hamburger menu now says when the person holding a task took it on.
  shipped · draft PR #235 · branch `166-claimed-time-row`
  touches: packages/shared/src/history.ts (new), packages/shared/src/index.ts,
    apps/web/src/App.tsx, scripts/claimed-since-sim-test.mjs (new), package.json,
    apps/web/CLAUDE.md, docs/adr/0002-task-handoff.md
  decisions: my brief's event table was WRONG — release-in-place writes
    TASK_RELEASED (task-service.ts:676), not TASK_UNCLAIMED. Verified. Worker
    deviated correctly and treats both as assignee-clearing.
  also: worker amended ADR-0002, whose rejected-alternatives said "nothing reads
    the history API today". Something does now.
  unverified: browser rendering across the three themes; panel edge-flip with the
    taller block. No new CSS, so theme-safe by construction but unobserved.

## Merged

#235 squash-merged to main as 6760e93 (user authorised). Local fast-forwarded.
`npm run lint` clean, `npm run test:all` exit 0. #166 auto-closed.
Branch `166-claimed-time-row` left on the remote, per the standing rule about
not deleting branches other PRs may be based on.

Queue after this run: #230 (startable now), #231/#232/#233 (blocked by #230),
#234 (blocked by #231+#233), #172 (parent, still mislabelled ready-for-agent).
Untriaged: #227, #228 — both need the user to run /triage.
